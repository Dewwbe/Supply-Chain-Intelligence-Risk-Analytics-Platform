"""Reusable profiling and data-quality checks.

Used directly by `notebooks/01_data_profiling.ipynb` (Phase 1) and reused by
`etl/validate/` (Phase 2), so the rules are defined once, not duplicated
between the notebook and the pipeline.
"""
