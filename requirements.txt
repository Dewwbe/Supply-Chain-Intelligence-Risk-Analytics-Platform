# --- API / web ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
slowapi==0.1.9
httpx==0.27.2
redis==5.0.8

# --- data / db ---
pandas==2.2.2
numpy==1.26.4
sqlalchemy==2.0.32
psycopg[binary]==3.2.1
tabulate==0.10.0

# --- data acquisition / profiling (Phase 1, see notebooks/01_data_profiling.ipynb) ---
# >=1.8.0 required for the new API-token auth (KAGGLE_API_TOKEN / ~/.kaggle/access_token),
# not just the legacy kaggle.json
kaggle==2.2.4
jupyter==1.1.1
ipykernel==6.29.5
# ydata-profiling is optional (PRD §Phase 1 tools) — heavy dependency, install
# only if you want its auto-generated HTML report:
#   pip install ydata-profiling==4.10.0

# --- analytics / ml ---
scipy==1.13.1
statsmodels==0.14.2
scikit-learn==1.5.1
xgboost==2.1.1

# --- big data / orchestration (introduced deliberately — see README §6) ---
pyspark==3.5.2
apache-airflow==2.10.1

# --- observability ---
structlog==24.4.0

# --- dev / quality ---
pytest==8.3.2
pytest-cov==5.0.0
black==24.8.0
ruff==0.6.4
mypy==1.11.2
pre-commit==3.8.0
