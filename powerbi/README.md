# Power BI

`gulfmart_supply_chain.pbix` (not committed — see .gitignore; distribute via
release assets). Five pages: Executive Overview, Sales & Demand, Inventory,
Supplier & Logistics, Scenario Simulator. Every measure must map to
`docs/kpi_dictionary.md`.
