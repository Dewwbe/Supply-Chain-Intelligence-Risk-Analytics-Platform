# Excel

Management-validation workbook (`kpi_validation.xlsx`) mirroring the Power BI
KPIs via PivotTables/XLOOKUP/SUMIFS, used to sanity-check the BI layer
independently. `scenario_analysis.xlsx` mirrors `/api/v1/scenario/simulate`.
