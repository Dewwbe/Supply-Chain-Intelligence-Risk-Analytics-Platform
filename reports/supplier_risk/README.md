# Supplier Risk Scores (Phase 7)

Generated by `notebooks/07_supplier_risk_scoring.ipynb` via
`src/supplier_risk/`.

- `supplier_risk_scores.csv` / `.md` — one row per supplier: `risk_score`
  (0-100) and `risk_level` (Low/Medium/High/Critical).

Weights and the risk-level thresholds are documented in
`src/supplier_risk/scoring.py`, not implied — see that file and
`docs/kpi_dictionary.md` §4 before changing either. Regenerate after any
change to the underlying `fact_shipments`/`fact_purchase_orders`/
`fact_returns` data (i.e. after `make etl`).
