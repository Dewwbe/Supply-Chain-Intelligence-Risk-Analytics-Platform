# Notebooks

Sequenced 01-09 per `docs/implementation_plan.md`. Each notebook should be
runnable top-to-bottom on a fresh clone with only `make etl` as a
prerequisite. Keep heavy computation in `src/`; notebooks call into it and
focus on exploration/narrative, not on reimplementing logic.
